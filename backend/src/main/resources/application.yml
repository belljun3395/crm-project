# application.yml has only common properties
spring:
    profiles:
        group:
            local:
                - local
            prd:
                - prd
            dev:
                - dev